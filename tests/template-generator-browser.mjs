// Playwright is isolated in tests/package.json for browser verification only.
// The generator's build and runtime have no Node dependency.
import {chromium, expect} from '@playwright/test';
import {spawn} from 'node:child_process';
import {mkdtemp, mkdir, readFile, writeFile} from 'node:fs/promises';
import {tmpdir} from 'node:os';
import {resolve} from 'node:path';
const root = resolve(import.meta.dirname, '..');
const output = resolve(root, process.env.RMP_GENERATOR_OUTPUT || 'build/template-generator-blazor');
const url = process.env.GENERATOR_URL || 'http://127.0.0.1:5188';
const apiOrigin = 'http://127.0.0.1:18680';
await mkdir(output, {recursive:true});
const data = await mkdtemp(resolve(tmpdir(), 'generator-blazor-api-'));
const server = spawn(process.env.RMP_SERVER_BIN || resolve(root, 'build/router-config/router-server.exe'),
  ['-listen','127.0.0.1:18681','-http-listen','127.0.0.1:18680','-repository-dir',data,'-tunnel-data-listen','127.0.0.1:18682','-tunnel-port-first','38300','-tunnel-port-last','38320'],
  {cwd:root, windowsHide:true, stdio:'pipe'});
let browser;
const errors = [], checks = [];
const check = name => { checks.push(name); console.log('PASS ' + name); };
async function api(path, method='GET', body) {
  const response = await fetch(apiOrigin + '/api/v1/' + path, {method, headers:{'Content-Type':'application/json','Idempotency-Key':crypto.randomUUID()}, ...(body ? {body:JSON.stringify(body)} : {})});
  const result = await response.json();
  if (!response.ok) throw Error(JSON.stringify(result));
  return result.data;
}
try {
  for (let attempt=0; attempt<100; attempt++) {
    try { await api('probe-templates'); break; } catch { if (attempt===99) throw Error('Go API did not start'); }
    await new Promise(resolve => setTimeout(resolve,100));
  }
  browser = await chromium.launch({headless:true,channel:process.env.RMP_BROWSER_CHANNEL || 'msedge'});
  const page = await browser.newPage({viewport:{width:1920,height:1080}});
  page.on('pageerror', error => errors.push(String(error)));
  page.on('console', message => { if (message.type()==='error') errors.push(message.text()); });
  page.setDefaultTimeout(12000);
  const tab = name => page.getByRole('tab',{name:new RegExp(name)}).click();
  const select = async key => { await page.locator('.attribute-item').filter({has:page.locator('code',{hasText:new RegExp('^'+key+'$')})}).click(); await expect(page.locator('.editor-heading > div > code')).toHaveText(key); };
  const confirm = async (name) => {
    const dialog=page.getByRole('dialog');
    await expect(dialog.locator('input,textarea')).toHaveCount(0);
    await dialog.getByRole('button',{name,exact:true}).click();
    await expect(dialog).toBeHidden();
  };
  const open = async json => {
    await page.locator('#project-file').setInputFiles({name:'import.project.json',mimeType:'application/json',buffer:Buffer.from(JSON.stringify(json))});
  };
  async function download(action) {
    const event=page.waitForEvent('download'); await action();
    const file=await event; return JSON.parse(await readFile(await file.path(),'utf8'));
  }
  await page.goto(url);
  await page.getByLabel('更多操作').click();
  await page.getByRole('button',{name:'加载示例',exact:true}).last().click();
  await expect(page.locator('.attribute-item')).toHaveCount(6);
  await select('memory_total');
  await page.getByRole('button',{name:'复制属性',exact:true}).click();
  await expect(page.getByLabel('属性标识',{exact:true})).toHaveValue('memory_total_copy');
  await page.getByLabel('模拟采集值',{exact:true}).fill('123');
  await select('memory_total');
  await expect(page.getByLabel('模拟采集值',{exact:true})).toHaveValue('262144');
  await select('memory_total_copy');
  await page.locator('.editor-heading').getByRole('button',{name:'删除',exact:true}).click();
  await expect(page.getByRole('dialog').getByRole('button',{name:'取消',exact:true})).toBeFocused();
  await page.keyboard.press('Escape');
  await expect(page.getByRole('dialog')).toBeHidden();
  await expect(page.locator('.attribute-item')).toHaveCount(7);
  await page.locator('.editor-heading').getByRole('button',{name:'删除',exact:true}).click();
  await confirm('删除');
  await expect(page.locator('.attribute-item')).toHaveCount(6);
  await page.getByLabel('搜索属性').fill('used');
  await expect(page.locator('.attribute-item')).toHaveCount(1);
  await page.getByLabel('搜索属性').fill('');
  check('attribute selection, independent copy, deletion confirmation and search');

  await select('used_percent');
  await page.getByLabel('属性标识',{exact:true}).fill('memory_total');
  await expect(page.locator('#attribute-key-error')).toContainText('重复');
  await page.getByLabel('属性标识',{exact:true}).fill('used_percent');
  await page.getByLabel('来源内容',{exact:true}).fill('memory_total / 0');
  await tab('预览与校验');await page.getByRole('button',{name:'其他信息',exact:true}).click();
  await expect(page.locator('tbody tr').filter({hasText:'used_percent'})).toContainText('除数');
  await tab('属性与公式');
  await page.getByLabel('来源内容',{exact:true}).fill('(memory_total - memory_free) / memory_total * 100');
  await tab('预览与校验');await page.getByRole('button',{name:'其他信息',exact:true}).click();
  await expect(page.locator('tbody tr').filter({hasText:'used_percent'})).toContainText('75');
  check('immediate field conflicts, formula failure and successful preview');

  const project = await download(() => page.keyboard.press('Control+s'));
  if (project.schema_version!==8 || project.attributes.length!==6) throw Error('Project download incomplete');
  await expect(page.locator('.document-state')).toContainText('已保存');
  await page.reload();
  await tab('属性与公式');
  await expect(page.locator('.attribute-item')).toHaveCount(6);
  await expect(page.getByLabel('模板名称',{exact:true})).toHaveValue(project.name);
  check('Ctrl+S project download and browser draft reload');

  await page.getByLabel('更多操作').click();
  await page.getByRole('button',{name:'条件示例',exact:true}).click();
  await confirm('替换');
  await select('wan_type');
  await expect(page.locator('.rule-row')).toHaveCount(3);
  await page.getByLabel('规则 2 显示文本',{exact:true}).fill('');
  await expect(page.locator('.rule-row').nth(1).locator('.field-error')).toContainText('显示文本');
  await page.getByLabel('规则 2 显示文本',{exact:true}).fill('5G');
  await page.getByLabel('下移规则 1',{exact:true}).click();
  await expect(page.getByLabel('规则 1 条件',{exact:true})).toHaveValue('wan_proto == 1');
  await page.getByLabel('上移规则 2',{exact:true}).click();
  await page.getByLabel('删除规则 1',{exact:true}).click();
  await page.getByRole('dialog').getByRole('button',{name:'取消',exact:true}).click();
  await expect(page.locator('.rule-row')).toHaveCount(3);
  await tab('预览与校验');await page.getByRole('button',{name:'其他信息',exact:true}).click();
  await expect(page.locator('tbody tr').filter({hasText:'wan_type'})).toContainText('4G');
  await expect(page.locator('tbody tr').filter({hasText:'wan_type'})).toContainText('命中规则 1');
  await tab('属性与公式'); await select('wan_proto');
  await page.getByLabel('模拟采集值',{exact:true}).fill('9');
  await tab('预览与校验');await page.getByRole('button',{name:'其他信息',exact:true}).click();
  await expect(page.locator('tbody tr').filter({hasText:'wan_type'})).toContainText('默认结果');
  check('condition editing, near-field validation, ordering, delete cancel, matched and fallback previews');

  await tab('属性与公式'); await select('wan_type');
  for (const [width,height,theme] of [[1920,1080,'Light'],[2560,1440,'Dark'],[3440,1440,'Light'],[900,760,'Dark']]) {
    await page.setViewportSize({width,height});
    await page.getByLabel('更多操作').click();
    await page.getByLabel('外观',{exact:true}).selectOption(theme);
    await expect(page.locator('html')).toHaveAttribute('data-theme',theme.toLowerCase());
    await page.screenshot({path:resolve(output,`workspace-${width}-${theme}.png`)});
    const geometry=await page.evaluate(() => ({w:innerWidth,h:innerHeight,body:document.documentElement.scrollWidth,
      shell:document.querySelector('.app-shell').getBoundingClientRect().toJSON(),bar:document.querySelector('.status-bar').getBoundingClientRect().toJSON(),
      grid:document.querySelector('.field-grid') && getComputedStyle(document.querySelector('.field-grid')).gridTemplateColumns}));
    if (geometry.body>width || Math.abs(geometry.bar.bottom-height)>1 || geometry.shell.width!==width) throw Error('Workspace overflow/layout: '+JSON.stringify(geometry));
  }
  await page.getByLabel('折叠或展开属性导航').click();
  await expect(page.locator('.workspace')).toHaveClass(/pane-collapsed/);
  await page.getByLabel('折叠或展开属性导航').click();
  await page.setViewportSize({width:1920,height:1080});
  check('1920/2560/3440 and narrow layouts, Light/Dark themes and collapsible navigation');

  await tab('属性与公式'); await select('wan_type');
  await tab('模板配置');await tab('内置监控');
  await expect(page.locator('.navigation-pane')).toHaveCount(0);
  await page.getByLabel('在模板中配置内置监控周期').check();
  await page.getByLabel('CPU上报周期',{exact:true}).fill('2');
  await page.getByLabel('自定义采集接口',{exact:true}).check();
  await page.getByLabel('采集接口名',{exact:true}).fill('eth0,br0');
  await page.getByLabel('出口IP探测周期',{exact:true}).fill('600');
  await page.getByLabel('存储上报周期',{exact:true}).fill('0');
  await tab('属性与公式');await expect(page.getByRole('heading',{name:'内置监控',exact:true})).toHaveCount(0);
  await page.locator('.advanced-section summary').click();
  await page.getByLabel('属性上报周期',{exact:true}).fill('7');
  const rulesProject = await download(() => page.keyboard.press('Control+s'));
  await tab('导出与发布');
  const runtime = await download(() => page.getByRole('button',{name:'导出运行模板',exact:true}).click());
  if (Object.keys(runtime.properties).length!==2 || !runtime.properties.wan_type.command.includes('awk')) throw Error('Runtime export mismatch');
  await page.getByLabel('管理服务器地址',{exact:true}).fill(apiOrigin);
  await page.getByRole('button',{name:'保存并连接',exact:true}).click();
  await expect(page.getByRole('button',{name:'发布为新模板',exact:true})).toBeEnabled();
  await page.getByRole('button',{name:'发布为新模板',exact:true}).click();
  await expect(page.locator('.template-table tbody tr')).toHaveCount(1);
  let item=(await api('probe-templates')).items[0];
  if (item.monitoring.network_interfaces!=='eth0,br0' || item.monitoring.egress_seconds!==600 || item.monitoring.cpu_seconds!==2 || item.monitoring.disk_seconds!==0 || item.properties.wan_type.interval_seconds!==7) throw Error('Monitoring cycles lost in publish');
  if (item.properties.wan_type.command!==runtime.properties.wan_type.command) throw Error('Go API command differs from export');
  await page.getByRole('button',{name:'更新已绑定模板',exact:true}).click(); await confirm('更新');
  await expect.poll(async () => (await api('probe-templates')).items[0].version).toBe(2);
  // A concurrent client update must surface a conflict rather than overwrite the newer version.
  item=(await api('probe-templates')).items[0];
  await api('probe-templates/'+item.template_id,'PUT',{name:item.name,properties:item.properties,monitoring:item.monitoring,version:2});
  await page.getByRole('button',{name:'更新已绑定模板',exact:true}).click();
  await page.getByRole('dialog').getByRole('button',{name:'更新',exact:true}).click();
  await expect(page.locator('.workspace-toast')).toContainText('冲突');
  await page.getByRole('dialog').getByRole('button',{name:'取消',exact:true}).click();
  await page.getByRole('button',{name:/刷新/}).first().click();
  await page.getByRole('button',{name:'绑定 '+item.name,exact:true}).click();
  await page.getByRole('button',{name:'更新已绑定模板',exact:true}).click(); await confirm('更新');
  await expect.poll(async () => (await api('probe-templates')).items[0].version).toBe(4);
  await page.getByRole('button',{name:'删除 '+item.name,exact:true}).click(); await confirm('删除');
  await expect(page.locator('.template-table tbody tr')).toHaveCount(0);
  check('runtime export and real Go API create/update/conflict/rebind/delete');

  // Current workspace export keeps the source graph and original binding.
  await open({workspace_version:3,serverUrl:apiOrigin,project:rulesProject,target:{origin:apiOrigin,id:item.template_id,version:4},savedAt:1});
  await confirm('替换');
  await expect(page.locator('.attribute-item')).toHaveCount(4);
  await tab('导出与发布');
  await expect(page.locator('.publishing-target')).toContainText(item.template_id);
  const v1={...project,schema_version:1};
  await open(v1); await expect(page.locator('.workspace-toast')).toContainText('不支持');
  await open(project); await confirm('替换');
  await expect(page.locator('.attribute-item')).toHaveCount(6);
  await page.locator('#project-file').setInputFiles({name:'invalid.json',mimeType:'application/json',buffer:Buffer.from('{broken')});
  await expect(page.locator('.workspace-toast')).toContainText('JSON');
  await expect(page.locator('.attribute-item')).toHaveCount(6);
  check('current workspace import, previous project rejection and invalid import protection');
  await page.getByRole('button',{name:'新建',exact:false}).first().click(); await confirm('替换');
  await page.getByLabel('模板名称',{exact:true}).fill('手动属性工程');
  await page.locator('.add-menu summary').click();
  await page.locator('.add-menu').getByRole('button',{name:/展示属性/}).click();
  await expect(page.locator('.attribute-item')).toHaveCount(1);
  await page.getByLabel('属性名称',{exact:true}).fill('WAN IP地址');
  await page.getByLabel('属性标识',{exact:true}).fill('wanip'); await expect(page.locator('.editor-heading > div > code')).toHaveText('wanip');
  await page.getByLabel('获取方式',{exact:true}).selectOption('Nvram');
  await page.getByLabel('来源内容',{exact:true}).fill('wan_ipaddr');
  await page.getByLabel('模拟采集值',{exact:true}).fill('192.168.1.1');
  await page.locator('.advanced-section summary').click();
  await page.getByLabel('采集超时',{exact:true}).fill('12');
  await expect(page.locator('.validation-status')).toContainText('校验通过');
  await page.locator('.add-menu summary').click();
  await page.locator('.add-menu').getByRole('button',{name:/虚拟属性/}).click();
  await expect(page.locator('.attribute-item')).toHaveCount(2);
  await page.getByLabel('属性名称',{exact:true}).fill('虚拟主机名');
  await page.getByLabel('属性标识',{exact:true}).fill('router_name');
  await page.getByLabel('获取方式',{exact:true}).selectOption('Uci');
  await page.getByLabel('来源内容',{exact:true}).fill('system.@system[0].hostname');
  await page.getByLabel('模拟采集值',{exact:true}).fill('router');
  await tab('预览与校验');await page.getByRole('button',{name:'其他信息',exact:true}).click();
  await expect(page.locator('tbody tr').filter({hasText:'wanip'})).toHaveCount(1);
  await expect(page.locator('tbody tr').filter({hasText:'wanip'})).toContainText('192.168.1.1');
  await tab('导出与发布');
  const manual=await download(() => page.getByRole('button',{name:'导出运行模板',exact:true}).click());
  if (manual.properties.wanip.timeout_seconds!==12 || manual.properties.wanip.source!=='nvram') throw Error('Manual source/timeout mismatch');
  await tab('属性与公式'); await select('wanip');
  await page.getByLabel('更多操作').click(); await page.getByLabel('外观',{exact:true}).selectOption('Light');
  await expect(page.locator('.document-state')).toContainText('已保存');
  await page.screenshot({path:resolve(output,'editor-final-1920.png')});
  check('new project and manual display/virtual attributes, NVRAM/UCI sources and timeout');
  // Edit the new presentation and physical-port schema through the actual controls.
  await tab('模板配置');await tab('分组与排序');
  await page.getByRole('button',{name:'添加分组',exact:true}).click();
  await page.getByLabel('分组名称',{exact:true}).fill('网络信息');
  await page.getByLabel('分组排序',{exact:true}).fill('20');
  await page.getByRole('navigation',{name:'属性分类',exact:true}).getByRole('button',{name:'自定义属性',exact:true}).click();
  await expect(page.locator('[data-field="wanip"]')).toHaveCount(1);
  await expect(page.locator('[data-field="router_name"]')).toHaveCount(0); // virtual attribute
  await page.getByLabel('搜索展示字段').fill('WAN');
  await expect(page.locator('.layout-fields tbody tr')).toHaveCount(1);
  await page.locator('[data-field="wanip"] select').selectOption('group_1');
  await tab('属性与公式');await select('wanip');
  await expect(page.getByLabel('所属分组',{exact:true})).toHaveValue('group_1');
  await page.getByLabel('组内顺序',{exact:true}).fill('10');
  await page.getByLabel('属性标识',{exact:true}).fill('wan_address');await page.getByLabel('属性名称',{exact:true}).click();
  await page.getByRole('button',{name:'管理分组与排序',exact:true}).click();
  await expect(page.locator('[data-field="wan_address"] select')).toHaveValue('group_1');
  await expect(page.locator('[data-field="wanip"]')).toHaveCount(0);
  await tab('属性与公式');await select('wan_address');
  await page.getByLabel('属性标识',{exact:true}).fill('wanip'); await expect(page.locator('.editor-heading > div > code')).toHaveText('wanip');
  await page.getByRole('button',{name:'复制属性',exact:true}).click();
  await expect(page.getByLabel('所属分组',{exact:true})).toHaveValue('group_1');
  await page.locator('.editor-heading').getByRole('button',{name:'删除',exact:true}).click();await confirm('删除');
  await tab('预览与校验');await expect(page.getByRole('navigation',{name:'属性分组'}).getByRole('button')).toHaveText(['系统信息','资源监控','网络信息','其他信息']);await page.getByRole('button',{name:'网络信息',exact:true}).click();await expect(page.locator('.preview-group')).toHaveAttribute('data-group','group_1');
  await tab('模板配置');await tab('分组与排序');
  await expect(page.getByRole('tab',{name:'接口显示名',exact:true})).toHaveCount(0);
  await expect(page.getByLabel('磁盘明细在属性页显示',{exact:true})).toBeChecked();
  await page.getByLabel('逻辑网口明细在属性页显示',{exact:true}).check();
  await page.getByRole('navigation',{name:'属性分类',exact:true}).getByRole('button',{name:'设备与系统',exact:true}).click();
  await page.getByLabel('搜索展示字段').fill('');
  await page.getByLabel('内核在属性页显示',{exact:true}).uncheck();
  const categories=page.getByRole('navigation',{name:'属性分类',exact:true});
  await categories.getByRole('button',{name:'CPU',exact:true}).click();
  const cpuRow=page.locator('[data-field="cpu_usage"]');
  await cpuRow.scrollIntoViewIfNeeded();
  const rowOrder=await page.locator('.layout-fields tbody tr').evaluateAll(rows=>rows.map(r=>r.dataset.field));
  const position=await cpuRow.boundingBox();
  await cpuRow.locator('select').selectOption('other');
  await expect(cpuRow.locator('select')).toHaveValue('other');
  if(JSON.stringify(rowOrder)!==JSON.stringify(await page.locator('.layout-fields tbody tr').evaluateAll(rows=>rows.map(r=>r.dataset.field))))throw Error('Editing group moved rows');
  if(Math.abs((await cpuRow.boundingBox()).y-position.y)>3)throw Error('Changing display group moved reading position');
  await categories.getByRole('button',{name:'磁盘',exact:true}).click();
  await page.getByLabel('显示存储空间页面',{exact:true}).uncheck();
  await page.screenshot({path:resolve(output,'classified-disk-visibility.png')});
  check('classified editing keeps row order and scroll position; storage page has an independent switch');

  await tab('预览与校验');await expect(page.locator('.preview-group td').filter({hasText:/^内核$/})).toHaveCount(0);
  const previewTable=page.locator('.preview-group table').first();
  const valueHeader=previewTable.locator('th').nth(2);
  await expect(previewTable.locator('td').first()).toHaveCSS('white-space','nowrap');
  await valueHeader.scrollIntoViewIfNeeded();
  const headerBounds=await valueHeader.boundingBox();
  await page.mouse.move(headerBounds.x+headerBounds.width-2,headerBounds.y+headerBounds.height/2);
  await page.mouse.down();await page.mouse.move(headerBounds.x+headerBounds.width-60,headerBounds.y+headerBounds.height/2,{steps:4});await page.mouse.up();
  await expect(previewTable.locator('tbody tr').first().locator('td').nth(2)).toHaveCSS('white-space','pre-wrap');
  const narrowed=await valueHeader.boundingBox();if(narrowed.width>=headerBounds.width-20)throw Error('Column did not resize');
  await valueHeader.focus();await page.keyboard.press('ArrowRight');
  await expect.poll(async()=>(await valueHeader.boundingBox()).width).toBeGreaterThan(narrowed.width);
  check('tables default to single line; pointer and keyboard resizing allow wrapping');
  await tab('模板配置');
  await tab('物理端口采集');
  await page.getByLabel('启用物理端口探测').check();
  await page.getByLabel('交换机采集方式',{exact:true}).selectOption('swconfig');
  await page.getByRole('button',{name:'添加物理端口映射',exact:true}).click();
  await expect(page.getByLabel('芯片端口号',{exact:true})).toHaveValue('');
  await page.getByLabel('交换机实例',{exact:true}).fill('switch0');
  await expect(page.getByRole('alert').filter({hasText:'芯片端口号'}).first()).toBeVisible();
  await page.getByLabel('芯片端口号',{exact:true}).fill('3');
  await page.getByLabel('端口显示名',{exact:true}).fill('LAN1');
  await page.getByLabel('端口用途',{exact:true}).selectOption('external');
  await page.getByText('高级设置与关联信息',{exact:true}).click();
  await page.getByLabel('物理端口标识',{exact:true}).fill('lan1');
  await page.getByLabel('系统接口',{exact:true}).fill('vlan3');
  await page.getByLabel('上联接口',{exact:true}).fill('eth0');
  await tab('导出与发布');
  const grouped=await download(()=>page.getByRole('button',{name:'导出运行模板',exact:true}).click());
  if(grouped.presentation.groups[0].name!=='网络信息'||grouped.presentation.fields.wanip.order!==10||'interface_aliases' in grouped.presentation||grouped.presentation.builtin_visibility.network!==true||grouped.presentation.fields.kernel.visible!==false||grouped.switch_probe.ports[0].uplink!=='eth0')throw Error('Presentation/switch runtime metadata missing');
  await page.getByRole('button',{name:'发布为新模板',exact:true}).click();
  await expect(page.locator('.template-table tbody tr')).toHaveCount(1);
  const managed=(await api('probe-templates')).items[0];
  if(managed.presentation.storage_visible!==false||managed.presentation.fields.wanip.group_id!=='group_1'||managed.switch_probe.ports[0].role!=='external')throw Error('Server dropped display/port schema');
  await page.getByText('型号与默认模板映射',{exact:true}).click();
  await page.getByRole('button',{name:'新建型号',exact:true}).click();
  await page.getByLabel('型号名称',{exact:true}).fill('中性固件路由器');
  await page.getByLabel('型号识别别名',{exact:true}).fill('board-neutral');
  await expect(page.getByLabel('型号默认模板',{exact:true}).locator('option').filter({hasText:'手动属性工程'})).toContainText('v1');
  await page.getByLabel('型号默认模板',{exact:true}).selectOption(managed.template_id);
  await page.getByRole('button',{name:'保存型号映射',exact:true}).click();
  await expect.poll(async()=>(await api('device-models')).items.length).toBe(1);
  const match=await api('device-models/match?name=BOARD-NEUTRAL');
  if(match.template_id!==managed.template_id)throw Error('Model alias/default mapping missing');
  await page.screenshot({path:resolve(output,'managed-model-mapping.png')});
  await tab('模板配置');await tab('物理端口采集');await page.screenshot({path:resolve(output,'managed-presentation.png')});
  check('groups, field order, visibility, switch mapping, model directory and default template through browser and real API');
  await page.getByLabel('交换机采集方式',{exact:true}).selectOption('dsa');
  await page.getByText('高级设置与关联信息',{exact:true}).click();
  await page.getByLabel('交换机实例',{exact:true}).fill('');await page.getByLabel('芯片端口号',{exact:true}).fill('');
  await page.getByLabel('系统物理接口',{exact:true}).fill('lan1');
  await tab('导出与发布');const dsa=await download(()=>page.getByRole('button',{name:'导出运行模板',exact:true}).click());
  if('port' in dsa.switch_probe.ports[0])throw Error('Unknown DSA chip port became zero');
  await page.getByRole('button',{name:'更新已绑定模板',exact:true}).click();await confirm('更新');
  await expect.poll(async()=>(await api('probe-templates')).items[0].version).toBe(2);
  if('port' in (await api('probe-templates')).items[0].switch_probe.ports[0])throw Error('Server invented a zero port');
  const redesigned=await download(()=>page.keyboard.press('Control+s'));
  await open({...redesigned,schema_version:5});await expect(page.locator('.workspace-toast')).toContainText('不支持');
  await open(redesigned);await confirm('替换');
  await tab('模板配置');await tab('物理端口采集');await expect(page.getByLabel('系统物理接口',{exact:true})).toHaveValue('lan1');
  await tab('分组与排序');await page.getByRole('navigation',{name:'属性分类',exact:true}).getByRole('button',{name:'自定义属性',exact:true}).click();await expect(page.locator('[data-field="wanip"] select')).toHaveValue('group_1');
  for(const [width,height,theme] of [[1920,1080,'Light'],[900,760,'Dark']]){
    await page.setViewportSize({width,height});await page.getByLabel('更多操作').click();await page.getByLabel('外观',{exact:true}).selectOption(theme);
    await page.screenshot({path:resolve(output,`configuration-${width}-${theme}.png`)});
    if(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth))throw Error('Configuration overflows viewport');
  }
  check('dedicated settings, shared attribute grouping, rename/copy/delete, grouped preview, nullable DSA port, current format and settings layouts');
  await tab('物理端口采集');
  await page.getByRole('button',{name:'载入 FNR100 五口配置（替换本页网口配置）',exact:true}).click();
  await page.getByLabel('配置模板名称',{exact:true}).fill('FNR100物理口样例');
  await expect(page.getByLabel('启用物理口流量',{exact:true})).toBeChecked();
  await expect(page.getByLabel('端口显示名',{exact:true})).toHaveCount(6);
  await expect(page.getByLabel('RX字节字段',{exact:true})).toHaveValue('RxGoodByte');
  await page.getByText('原始输出解析预览',{exact:true}).click();
  await page.getByLabel('端口计数样本',{exact:true}).fill('Port 1 MIB counters\nRxGoodByte: 9007199254740993\nTxByte: 18446744073709551615\n');
  await page.getByRole('button',{name:'解析计数样本',exact:true}).click();
  await expect(page.getByText('单口MIB样本 · RX 9007199254740993 B · TX 18446744073709551615 B',{exact:true})).toBeVisible();
  await page.getByLabel('端口计数样本',{exact:true}).fill('recv_good: 123\nTxByte: 456');
  await page.getByRole('button',{name:'解析计数样本',exact:true}).click();
  await expect(page.getByRole('alert').filter({hasText:'包计数不能代替字节'})).toBeVisible();
  for(const [width,height,theme] of [[1920,1080,'Light'],[900,760,'Dark']]){
    await page.setViewportSize({width,height});await page.getByLabel('更多操作').click();await page.getByLabel('外观',{exact:true}).selectOption(theme);
    await page.screenshot({path:resolve(output,`physical-counters-${width}-${theme}.png`)});
    if(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth))throw Error('Physical counter settings overflow viewport');
  }
  await tab('导出与发布');
  const portRuntime=await download(()=>page.getByRole('button',{name:'导出运行模板',exact:true}).click());
  if(portRuntime.switch_probe.counters.bits!==64||portRuntime.switch_probe.ports.map(p=>p.port).join(',')!=='1,2,3,4,5,0')throw Error('Board mapping/counters missing');
  await page.getByRole('button',{name:'发布为新模板',exact:true}).click();
  await expect.poll(async()=>(await api('probe-templates')).items.length).toBe(2);
  const physical=(await api('probe-templates')).items.find(t=>t.switch_probe?.counters);
  if(physical.switch_probe.counters.rx_field!=='RxGoodByte'||physical.switch_probe.ports[4].display_name!=='WAN')throw Error('Server lost physical profile');
  check('FNR100 profile, exact 64-bit sample preview, invalid counter rejection, layouts, export and real API publication');
  await tab('模板配置');await tab('邻居发现');
  await page.getByLabel('启用邻居发现 · 发现本机网络设备',{exact:true}).check();
  await expect(page.locator('.neighbor-editor details').first()).not.toHaveAttribute('open','');
  await page.locator('.neighbor-editor summary').filter({hasText:'高级设置'}).click();
  await page.getByRole('button',{name:'添加高级域',exact:true}).click();
  await page.getByLabel('原始Linux接口名',{exact:true}).fill('br0');
  await page.getByLabel('内部域ID',{exact:true}).fill('local');
  await page.getByRole('button',{name:'添加高级域',exact:true}).click();
  await page.getByLabel('内部域ID',{exact:true}).nth(1).fill('lan');
  await page.getByLabel('邻居scope',{exact:true}).nth(1).selectOption('lan');
  await page.getByLabel('原始Linux接口名',{exact:true}).nth(1).fill('br0');
  await page.getByLabel('计入LAN清单的端口',{exact:true}).nth(1).fill('lan1,lan2');
  for(const [width,height,theme] of [[1920,1080,'Light'],[900,760,'Dark']]){
    await page.setViewportSize({width,height});await page.getByLabel('更多操作').click();await page.getByLabel('外观',{exact:true}).selectOption(theme);
    await expect(page.locator('html')).toHaveAttribute('data-theme',theme.toLowerCase());
    await page.locator('.editor-section h3').scrollIntoViewIfNeeded();
    await page.screenshot({path:resolve(output,`neighbors-${width}-${theme}.png`)});
    if(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth))throw Error('Neighbor editor overflows viewport');
  }
  await tab('导出与发布');
  const neighbors=await download(()=>page.getByRole('button',{name:'导出运行模板',exact:true}).click());
  if(neighbors.neighbor_probe.domains[0].scope!=='broadcast'||neighbors.neighbor_probe.domains[1].ports.join(',')!=='lan1,lan2')throw Error('Neighbor configuration missing');
  await page.getByRole('button',{name:'更新已绑定模板',exact:true}).click();await confirm('更新');
  await expect.poll(async()=>(await api('probe-templates')).items.find(t=>t.template_id===physical.template_id).version).toBe(2);
  if((await api('probe-templates')).items.find(t=>t.template_id===physical.template_id).neighbor_probe.domains.length!==2)throw Error('Server lost neighbor domains');
  const neighborProject=await download(()=>page.keyboard.press('Control+s'));
  await open(neighborProject);await confirm('替换');await tab('模板配置');await tab('邻居发现');
  await page.locator('.neighbor-editor summary').filter({hasText:'高级设置'}).click();
  await expect(page.getByLabel('原始Linux接口名',{exact:true})).toHaveCount(2);
  await expect(page.getByLabel('计入LAN清单的端口',{exact:true}).nth(1)).toHaveValue('lan1,lan2');
  check('neighbor domains share interface, preserve distinct scopes and LAN ports across browser edit, project reload, export and API publication');
  if (errors.length) throw Error('Browser errors: '+errors.join('\n'));
  await writeFile(resolve(output,'browser-results.json'),JSON.stringify({checks,errors},null,2));
  console.log(`PASS ${checks.length} browser scenario groups; no browser errors`);
} catch(error) {
  const failedPage=browser?.contexts()[0]?.pages()[0];
  if(failedPage){await failedPage.screenshot({path:resolve(output,"failure.png")});await writeFile(resolve(output,"failure.html"),await failedPage.content());}
  throw error;
} finally {
  await browser?.close();
  server.kill();
}
