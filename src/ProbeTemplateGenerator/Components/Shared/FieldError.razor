@if (!string.IsNullOrWhiteSpace(Message))
{
    <span id="@Id" class="field-error" role="alert">@Message</span>
}
@code {
    [Parameter] public string? Message { get; set; }
    [Parameter] public string? Id { get; set; }
}
